{
  "name": "users-maca5-codes-benefactor-cc",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "test": "node --test workers/**/*.test.js",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "bash scripts/deploy.sh"
  },
  "dependencies": {
    "astro": "^7.0.7"
  }
}
